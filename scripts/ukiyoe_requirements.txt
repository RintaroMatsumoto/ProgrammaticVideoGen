# Ukiyoe pipeline dependencies
# Install: pip install -r scripts/ukiyoe_requirements.txt

# --- Core (MVP path) ---
requests>=2.31
Pillow>=10.0
anthropic>=0.40.0
python-dotenv>=1.0

# --- Full path (optional, heavy) ---
# SAM2: installed from source (see segment_layers.py)
# torch>=2.3               # GPU recommended
# torchvision>=0.18
# transformers>=4.45
# accelerate>=0.34

# Depth Anything V2
# Same torch stack; model loaded via transformers pipeline

# VOICEVOX: external HTTP service (run voicevox_engine separately)
# FLUX inpaint: diffusers (optional Phase 2)
# diffusers>=0.30
